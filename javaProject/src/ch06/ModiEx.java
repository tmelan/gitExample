package ch06;
public class ModiEx {
	//접근제어지정자 (Access Modifier)
	//public 모든 위치 참조 가능 protected 같은 패키지 또는 다른 패키지라도 상속관계가 있을 때 사용
	//private default 같은 패키지내에서만 사용 가능 실제로는 적지 않음 
	
	protected String name = "kim";
}
